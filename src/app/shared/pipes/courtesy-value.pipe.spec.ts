import { CourtesyValuePipe } from './courtesy-value.pipe';

describe('CourtesyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CourtesyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
